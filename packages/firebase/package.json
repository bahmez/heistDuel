{
  "name": "@repo/firebase",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "build": "echo \"firebase: no build step\"",
    "dev": "echo \"firebase: no dev step\"",
    "typecheck": "tsc --noEmit",
    "lint": "echo \"firebase: no lint step\"",
    "test": "echo \"firebase: no test step\""
  },
  "dependencies": {
    "@repo/shared": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.7.3"
  }
}
