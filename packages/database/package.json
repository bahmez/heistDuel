{
  "name": "@repo/database",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "build": "echo \"database: no build step\"",
    "dev": "echo \"database: no dev step\"",
    "typecheck": "tsc --noEmit",
    "lint": "echo \"database: no lint step\"",
    "test": "echo \"database: no test step\""
  },
  "dependencies": {
    "@repo/shared": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.7.3"
  }
}
