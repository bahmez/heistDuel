[package]
name = "heist"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
soroban-sdk = "25.1.1"
soroban-poseidon = "25.0.0"

[dev-dependencies]
soroban-sdk = { version = "25.1.1", features = ["testutils"] }
